// map controller
// public/map.js

findMate.controller('profileController', ['$scope', '$http', function($scope, $http) {
    /*$scope.getFBData = function () {
        FB.api('/me', function(response) {
          fbinfo = new Array();
          fbinfo[0] = response.id;
          fbinfo[1] = response.first_name;
          fbinfo[2] = response.last_name;
          fbinfo[3] = response.email;

          var im = document.getElementById("profile-image").setAttribute("src", "http://graph.facebook.com/" + id + "/pciture?type=normal");
        });
    };

    $scope.$watch('$viewContentLoaded', function() {
        $scope.getFBData();
    });

   */
}]);